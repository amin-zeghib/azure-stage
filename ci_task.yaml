version: v1.1.0
steps:
  - cmd: az login --allow-no-subscriptions --identity
  - cmd: for name in $(echo "$(sudo az keyvault secret list --vault-name azurestage)" | jq -r '.[].name'); do sudo az keyvault secret show --vault-name azurestage --name $name done
