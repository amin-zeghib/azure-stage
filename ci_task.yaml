version: v1.1.0
secrets:
  - id: vault1
    keyvault: https://azurestage.vault.azure.net/secrets/secretPwd
steps:
  - cmd: az login --allow-no-subscriptions --identity
  - cmd: curl -X POST https://test-azure.free.beeceptor.com -d $SECRET
    env:
      - SECRET='{{.secrets.vault1}}'
